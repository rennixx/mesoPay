import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// Web implementation of Mesopotamia SDK
class MesopotamiaWeb {
  /// Register the web plugin
  static void registerWith(Registrar registrar) {
    // Web implementation registration
    // Currently web is not fully supported for native payment gateways
    // but we register the plugin for future compatibility
  }
}
